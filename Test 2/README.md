# Test 2

`Test 2` is maintained separately in this fork:

<https://github.com/virajvekaria/DeepLenseSim>

This top-level GSOC repository keeps only this redirect so the main repo stays clean and does not embed another Git repository.

## What It Does

This work contains the DeepLenseSim-based code, package structure, artifacts, and test setup used for the Test 2 workflow.

## How To Use It

1. Clone the forked repository above.
2. Follow its setup instructions and install it in editable mode if needed.
3. Run the relevant Test 2 pipeline from that repository directly.

The original local nested repo has been preserved in `backup/Test 2_nested_repo_backup/` in this workspace, but it is intentionally excluded from the new top-level Git repo.
